# Service Health Monitor v2.0

Console application that simulates API server monitoring.
Parses log lines, aggregates stats in real time, saves report to file.

## Usage

python service_health_monitor_v2.0.py

## Log format

/api/v1/login 200 150ms

## Commands
- Enter log lines one by one
- Type `stop` to generate report
